services:
  - type: web
    name: betting-bot-prov2
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
buildCommand: |
  pip install -r requirements.txt
  playwright install
